# 🔧 Backend Environment Variables - TEMPLATE
# Copia este archivo como .env (desarrollo) o .env.production (producción)
# y configura las variables según tu entorno

# 🌐 Configuración del Servidor
# Puerto donde correrá el servidor
PORT=3001
# Entorno de ejecución
NODE_ENV=development

# 🔐 Configuración de CORS
# Origen permitido para requests (frontend URL)
CORS_ORIGIN=http://localhost:5173
# Permitir envío de cookies/credentials
CORS_CREDENTIALS=true

# 🎭 Configuración de Playwright
# Saltar descarga de navegadores (usar solo si ya están instalados)
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=false
# Ruta específica del navegador (opcional)
PLAYWRIGHT_BROWSER_PATH=
# Ejecutar navegadores en modo headless
HEADLESS=true

# ⚡ Configuración de Scraping
# Delay entre requests para evitar rate limiting (ms)
SCRAPING_DELAY=1000
# Timeout total para operaciones de scraping (ms)
SCRAPING_TIMEOUT=30000
# Máximo número de fuentes a scrapear simultáneamente
MAX_CONCURRENT_SOURCES=5
# Delay adicional por rate limiting (ms)
RATE_LIMIT_DELAY=2000

# ✅ Configuración de Validación de Proxies
# Timeout para probar cada proxy (ms)
VALIDATION_TIMEOUT=10000
# Número de reintentos por proxy fallido
VALIDATION_RETRIES=2
# Máximo número de validaciones simultáneas
MAX_CONCURRENT_VALIDATIONS=5

# 📊 Configuración de Logs
# Nivel de logging: debug, info, warn, error
LOG_LEVEL=info
# Días de retención de logs
LOG_RETENTION_DAYS=7
# Tamaño máximo de logs en memoria (MB)
LOG_MAX_SIZE=100

# 🔄 Configuración de Cache
# Tiempo de vida del cache (ms)
CACHE_TTL=300000
# Tamaño máximo del cache (items)
CACHE_MAX_SIZE=1000

# 🔐 Configuración de Seguridad
# Confiar en headers de proxy (para load balancers)
TRUST_PROXY=false
# Tamaño máximo de request body
MAX_REQUEST_SIZE=10mb

# 📡 Configuración de SSE (Server-Sent Events)
# Intervalo de heartbeat para mantener conexión (ms)
SSE_HEARTBEAT_INTERVAL=30000
# Timeout para conexiones SSE inactivas (ms)
SSE_CONNECTION_TIMEOUT=300000

# 🌐 Configuración de Red
# User-Agent para requests HTTP
HTTP_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
# Timeout para requests HTTP externos (ms)
HTTP_REQUEST_TIMEOUT=15000

# 🎯 Configuración de Fuentes de Proxies
# URLs específicas de fuentes (opcional, usa defaults si no se especifica)
# PROXY_SOURCE_URL_1=https://custom-proxy-source.com/list
# PROXY_SOURCE_URL_2=https://another-source.com/api/proxies

# 🧪 Configuración de Testing
# Habilitar endpoints de testing
ENABLE_TEST_ENDPOINTS=true
# Generar datos mock para testing
ENABLE_MOCK_DATA=false
